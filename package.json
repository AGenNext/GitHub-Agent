{
  "name": "agennext-github-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable Probot-based GitHub agent for editorial, publishing, issue, and PR workflows.",
  "type": "module",
  "scripts": {
    "start": "probot run ./src/index.js",
    "lint": "node --check src/index.js && node --check src/skills/editorialReview.js && node --check src/utils/issue.js"
  },
  "dependencies": {
    "probot": "latest",
    "js-yaml": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
